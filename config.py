# This file will contain the project configurations

# Note: This file may change when the backend folder and files are made and implimented

# Importing the necessary libraries
import os
from dotenv import load_dotenv

load_dotenv()

token = os.getenv("DISCORD_TOKEN")

